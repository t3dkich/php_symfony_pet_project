{% extends 'base.html.twig' %}

{% block body %}

    {% for msg in app.session.flashbag.get('info') %}
        <div class="alert alert-danger">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">×</a>
            {{ msg }}
        </div>
    {% endfor %}

    <div class="container body-content span=2 offset=2 center">
        <div class="well">
            <div class="container body-content span=8 offset=2">
                <div class="well">
                    <form class="form-horizontal" method="post">
                        <div class="form-group">
                            <label for="user[email]">Email address</label>
                            <input type="email" class="form-control" name="user[email]" id="user[email]" aria-describedby="emailHelp"
                                   placeholder="Enter email">
                            <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone
                                else.
                            </small>
                        </div>
                        <div class="form-group">
                            <label class="col-sm-4 control-label" for="user[name]">Your Name</label>
                            <input type="text" class="form-control" name="user[name]" id="user[name]" placeholder="Enter your name">
                        </div>
                        <div class="form-group">
                            <label class="col-sm-4 control-label" for="user[password][first]">Password</label>
                            <input type="password" class="form-control" name="user[password][first]" id="user[password][first]" placeholder="Password">
                        </div>
                        <div class="form-group">
                            <label class="col-sm-4 control-label" for="user[password][second]">Repeat Password</label>
                            <input type="password" class="form-control" name="user[password][second]" id="user[password][second]"
                                   placeholder="Repeat Password">
                        </div>
                        {{ form_row(form._token) }}
                        <button type="submit" class="btn btn-primary">Register</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
